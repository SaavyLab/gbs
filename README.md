# gbs

Minimal terminal UI for browsing and switching local git branches. Built with ratatui + crossterm.

## Usage
```
cargo run
```
Use `j/k` or arrow keys to move, `Enter` to switch, `q` or `Esc` to quit.

## License
MIT
